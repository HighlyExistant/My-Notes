Used primarily in video games to separate state within various ==**entities**==, each containing a list of ==**components**==, which use ===**systems**=== for their state.
# Entities
These are usually just Nonrepeatable ID's used within the game. These are typically denoted by an unsigned 128 bit value. These entity ID's are used to query specific entities.